import StandardWrapperLayout from './StandardWrapperLayout'

export const generated = (args) => {
  return <StandardWrapperLayout {...args} />
}

export default {
  title: 'Layouts/StandardWrapperLayout',
  component: StandardWrapperLayout,
}
